en:
  errors:
    format: "%{message}"
  activerecord:
    models:
      student: 学生
      group: 班级
    attributes:
      group:
        name: 班级名称
    errors:
      models:
        group:
          attributes:
            name:
              blank: "没填%{attribute}"
              taken: "该%{attribute}已存在"
